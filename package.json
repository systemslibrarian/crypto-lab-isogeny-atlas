{
  "name": "crypto-lab-isogeny-atlas",
  "version": "1.0.0",
  "description": "Browser demo of the supersingular isogeny graph over GF(431^2) — a real, live-computed atlas of the curves, the paths, and the seven foremost open problems of isogeny-based cryptography.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:a11y": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
